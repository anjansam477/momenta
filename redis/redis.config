port 6379
bind 0.0.0.0
protected-mode no
save 60 1000
appendonly yes
# Bound memory so a spike can't OOM-kill the container; evict least-recently-used
# keys instead. Sized under the 2G container limit set in docker-compose.
maxmemory 1500mb
maxmemory-policy allkeys-lru
