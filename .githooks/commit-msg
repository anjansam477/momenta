#!/bin/sh
# Block any commit that adds a Co-authored-by trailer. (Version-controlled so it
# survives clones once `git config core.hooksPath .githooks` is set.)
if grep -qi "co-authored-by" "$1"; then
  echo "ERROR: Co-authored-by not allowed."
  exit 1
fi
